#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@microsoft/api-extractor/bin/api-extractor" "$@"
  ret=$?
else 
  node  "$basedir/../@microsoft/api-extractor/bin/api-extractor" "$@"
  ret=$?
fi
exit $ret
